{
  "name": "researchbox-worker",
  "version": "1.0.0",
  "description": "ResearchBox AI Proxy - Cloudflare Worker 版本（替代 Python FastAPI）",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "itty-router": "^4.0.13"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
