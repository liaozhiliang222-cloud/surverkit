# 生产环境变量模板
# 部署到 Cloudflare Pages 时，将此文件的变量配置到 Pages 项目的
# Settings > Environment Variables（Production 环境）

# AI 代理 Cloudflare Worker 地址
# 部署 Worker 后替换为你的实际地址
VITE_AI_API_URL=https://researchbox-ai-proxy.your-subdomain.workers.dev

# ASR Agent 保留本地（云端不使用）
VITE_ASR_API_URL=http://127.0.0.1:8765
