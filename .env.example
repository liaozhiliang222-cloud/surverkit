# ============================================================
# ResearchBox 前端环境变量示例
#
# 同源部署模式（推荐，单 Worker 部署）：
#   - 不需要设置 VITE_AI_API_URL，前端默认走 /api 相对路径
#   - 前端和 API 部署在同一个 Cloudflare Worker 上
#
# 本地开发模式：
#   - 复制为 .env.local，设置 VITE_AI_API_URL 指向本机 Python 代理
# ============================================================

# AI 代理地址
# - 同源部署（推荐）：留空或设为 /api，前端走相对路径
# - 本地开发：http://127.0.0.1:8766（连本机 Python 代理）
# - 旧版分离部署：https://researchbox-ai-proxy.<your-subdomain>.workers.dev
# VITE_AI_API_URL=/api

# ASR Agent 地址（始终本地，不对外暴露）
# 云端部署时此项不生效（ASR 入口仅在本地开发模式显示）
VITE_ASR_API_URL=http://127.0.0.1:8765
